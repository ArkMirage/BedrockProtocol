// <auto-generated>
//     由 Protocol.Gen 自动生成, 请勿手动修改
// </auto-generated>
using System;
using System.Buffers.Binary;
using System.Collections.Generic;
using System.IO;
using System.Numerics;
using OneOf;
using Protocol.Utility;
using Protocol.Utility.IO;

namespace Protocol.Types
{
	public class CommandOutput
	{
		public Protocol.CommandOutputType OutputType { get; set; }
		public uint SuccessCount { get; set; }
		public List<CommandOutputMessage> OutputMessages { get; set; } = new List<CommandOutputMessage>();
		public Optional<string> DataSet { get; set; } = new Optional<string>();

		public void Read(MemoryStreamReader reader)
		{
			OutputType = EnumCodec.FromString<Protocol.CommandOutputType>(reader.ReadLengthPrefixedString());
			SuccessCount = reader.ReadUInt32();
			OutputMessages = reader.ReadSlice(() => { var _Item = new CommandOutputMessage(); _Item.Read(reader); return _Item; });
			if (reader.ReadByte() != 0)
			{
				DataSet = new Optional<string>(reader.ReadLengthPrefixedString());
			}
		}

		public void Write(MemoryStreamWriter writer)
		{
			writer.WriteLengthPrefixedString(EnumCodec.ToString(OutputType));
			writer.WriteUInt32(SuccessCount);
			writer.WriteSlice(OutputMessages, v => v.Write(writer));
			if (DataSet != null && DataSet.HasValue)
			{
				writer.WriteByte(1);
				writer.WriteLengthPrefixedString(DataSet.Value);
			}
			else
			{
				writer.WriteByte(0);
			}
		}
	}
}
